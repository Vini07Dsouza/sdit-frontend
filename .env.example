# SDIT AI Assistant Backend Configuration
# URL of the Python Flask backend service (e.g., http://localhost:5000)
VITE_API_BASE_URL=

# Set to 'true' to use the client-side mock chat fallback when the backend is offline
VITE_USE_MOCK_CHAT=true
